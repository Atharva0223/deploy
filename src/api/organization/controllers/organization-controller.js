const {client} = require("../../../../config/pg");

module.exports = {
    //Get org detail
    async delete(ctx) {
      try {
        
      } catch (error) {
        console.log(error);
      }
    },
};