'use strict';

/**
 * store-notification router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::store-notification.store-notification');
